<% if object.errors.any? %>
  <div id="error_explanation" class="flash-field">
    <div class="alert alert-error">
      <%= pluralize(object.errors.count, "error") %> prohibited this task from being saved:
    </div>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
