<%= form_for(@system_example) do |f| %>
  <% if @system_example.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@system_example.errors.count, "error") %> prohibited this system_example from being saved:</h2>

      <ul>
      <% @system_example.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
